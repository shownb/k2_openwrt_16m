name: K2

on:
  repository_dispatch:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: env prepare
      run: |
        sudo -E apt-get update
        sudo -E apt-get install build-essential libncurses5-dev libncursesw5-dev zlib1g-dev gawk git gettext libssl-dev xsltproc wget unzip python
    - name: run compile.sh
      run: |
        sudo chmod +x ./compile.sh
        ./compile.sh
    - name: Assemble artifact
      run: |
        rm -rf ./artifact/
        mkdir -p ./artifact/
        cp ./openwrt-imagebuilder-21.02.0-rc4-ramips-mt7620.Linux-x86_64/target/linux/ramips/image/mt7620.mk ./artifact/
        cp ./openwrt-imagebuilder-21.02.0-rc4-ramips-mt7620.Linux-x86_64/target/linux/ramips/dts/mt7620a_phicomm_psg1218.dtsi ./artifact/
        find ./ -name "*sysupgrade*bin" | xargs -i mv -f {} ./artifact/
    - name: Upload artifact
      uses: actions/upload-artifact@master
      with:
        name: OpenWrt firmware
        path: ./artifact/
